如果要在shell中的一行执行多个指令，可以在多个指令之间使用分号“;”隔开，如：
$ cmd1; cmd2

如果后续指令的执行还需要考虑前面指令的执行结果正确与否，可以使用“&&”或“||”来进行，如：
$ cmd1 && cmd2
上面的指令如果cmd1的执行结果OK（可以通过指令回传值 $? 查看），则继续执行cmd2；否则，不执行cmd2

$ cmd1 || cmd2
上面的指令如果cmd1的执行结果OK（可以通过指令回传值 $? 查看），则不执行cmd2；否则，继续执行cmd2